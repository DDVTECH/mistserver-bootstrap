#
# Copy to project root as ".env" and adjust values
#

# Optional domain for public HTTPS via Caddy (e.g., stream.example.com)
# Leave empty to run HTTP-only on :80 without certificates.
DOMAIN=

# Mist admin credentials
ADMIN_USER=admin
ADMIN_PASSWORD=admin

# Bandwidth accounting
# Exclude local/private subnets from bandwidth stats (true/false)
BANDWIDTH_EXCLUDE_LOCAL=true

# Load balancing data (not important, unless using the load balancer)
LOCATION_NAME=
LOCATION_LAT=
LOCATION_LON=
BANDWIDTH_LIMIT_MBIT=1000   # Limit in megabits per second (integer)

# Prometheus path (leave as "metrics" to match Prometheus config)
PROMETHEUS_PATH=metrics

